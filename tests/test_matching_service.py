"""
ЗАГЛУШКА тестов (описание кейсов, которые нужно покрыть перед проду).
Реализация через pytest + fixtures, сбрасывающие in-memory репозитории
между тестами (сейчас репозитории — синглтоны на уровне модуля, для
тестов стоит добавить fixture, которая их переинициализирует).

Кейсы для matching_service.buy_exact_quantity:
  1. Один продавец полностью закрывает потребность — простой happy path.
  2. Потребность закрывается несколькими продавцами (композитный вексель
     из >1 SimpleVoucher) — проверить сортировку по цене (сначала дешёвые).
  3. min_deal_quantity одного из объявлений не позволяет "доткнуть" остаток
     ровно — алгоритм должен либо добрать до min (если это не критично),
     либо пропустить объявление и попробовать следующее по цене.
  4. max_deal_quantity ограничивает объём одной сделки — необходимо
     несколько сделок с ОДНИМ и тем же продавцом/объявлением (в текущей
     версии это не поддержано в одном проходе — зафиксировать как
     TODO/расширение: повторный проход по тому же объявлению).
  5. Суммарно на рынке недостаточно УЕ -> InsufficientMarketSupplyError
     с корректным best_available.
  6. Фильтр по характеристикам (project_name = "X") отсекает неподходящие
     объявления.

Кейсы для matching_service.invest_amount:
  7. Весь бюджет тратится на одно объявление.
  8. Бюджет распределяется по нескольким объявлениям от дешёвых к дорогим.
  9. Остаток бюджета, не влезающий в min_deal_quantity ни одного оставшегося
     объявления, не тратится (leftover) — проверить, что это не падение,
     а тихий недорасход.

Кейсы для voucher_service:
  10. issue_simple_voucher корректно замораживает УЕ в RegistryClient
      (available_quantity уменьшается сразу после выпуска векселя).
  11. redeem_composite_voucher переносит УЕ на баланс покупателя
      (get_balance(buyer_account) после redeem содержит нужное количество).
  12. Попытка обналичить чужой composite_voucher -> VoucherNotFoundError.

Кейсы для listing_service / seller_capacity_service:
  13. Продавец не может выставить объявление на объём больше, чем у него
      есть в реестре -> InsufficientSellerCapacityError.
  14. Продавец не может выставить второе объявление, которое в сумме с
      первым (активным) превышает его реальный остаток.
"""
